@{
     Layout = "~/Views/Shared/_Layout.cshtml";
     ViewBag.Title = "News";
     if (Session["Username"] != null)
     {
          var user1 = System.Web.HttpContext.Current.GetMySessionObject();

          if (user1.Level != URole.Moderator)
          {
               Response.Redirect("~/Home/Index");
          }
     }
     if (Session["Username"] == null)
     {
          Response.Redirect("~/Login/Index");
     }
}

@using System.Web.Optimization
@using eUseControl.Domain.Enums
@using eUseControl.Extension

<!DOCTYPE html>
<html>
<head>
     <title>Car and Driver News</title>
     <style>
          /* Add your custom CSS styles here */
          body {
               font-family: Arial, sans-serif;
          }

          header {
               background-color: #333;
               color: #fff;
               padding: 20px;
               text-align: center;
          }

          nav {
               background-color: #000;
               color: #fff;
               padding: 10px;
               text-align: center;
          }

          main {
               max-width: 1200px;
               margin: 0 auto;
               padding: 20px;
          }

          .article {
               margin-bottom: 40px;
          }

          .article-card {
               background-color: #f4f4f4;
               padding: 20px;
               border-radius: 5px;
          }

               .article-card img {
                    max-width: 100%;
                    height: auto;
                    margin-bottom: 10px;
               }

               .article-card h2 {
                    color: #333;
                    margin-top: 0;
               }

               .article-card p {
                    color: #666;
               }
     </style>
</head>
<body>
     <header>
          <h1>Car and Driver News</h1>
     </header>
     <main>
          <div class="article">
               <div class="article-card">
                    <img src="~/assets/img/news2.jpg" alt="Car 1">
                    <h2>Mercedes are starting new line of cars</h2>
                    <p>Article content</p>
               </div>
          </div>
          <div class="article">
               <div class="article-card">
                    <img src="~/assets/img/news1.jpg" alt="Car 2">
                    <h2>The World Best Selling Sports Car</h2>
                    <p>Article content goes here...</p>
               </div>
          </div>
          <!-- Add more articles as needed -->
     </main>
</body>
</html>
